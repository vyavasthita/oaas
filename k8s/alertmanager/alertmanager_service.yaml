apiVersion: v1
kind: Service
metadata:
  name: alertmanager
  namespace: oaas-observability
  labels:
    app: alertmanager
spec:
  selector:
    app: alertmanager
  ports:
    - name: http
      port: 9093
      targetPort: 9093
  type: ClusterIP
