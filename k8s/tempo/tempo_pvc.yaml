apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tempo-data
  namespace: oaas-observability
  labels:
    app: tempo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
